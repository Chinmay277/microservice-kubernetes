spring.application.name=notification-service

# ------------------------
# Server Configuration
# ------------------------
server.port=8080

# ------------------------
# Spring Datasource (PostgreSQL Docker)
# ------------------------
spring.datasource.url=jdbc:postgresql://postgres-db:5432/notificationdb
spring.datasource.username=postgres
spring.datasource.password=user
spring.datasource.driver-class-name=org.postgresql.Driver

# ------------------------
# JPA / Hibernate
# ------------------------
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ------------------------
# Eureka Client Configuration
# ------------------------
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# ------------------------
# Kafka Configuration (Docker)
# ------------------------
spring.kafka.bootstrap-servers=kafka:9092
spring.kafka.consumer.group-id=notification-group
spring.kafka.consumer.properties.spring.json.trusted.packages=com.utility.workorder_service.events,java.util,java.lang,*
spring.kafka.consumer.properties.spring.json.value.default.type=com.utility.workorder_service.events.WorkOrderEventDto
spring.kafka.consumer.properties.spring.json.use.type.headers=false

spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
